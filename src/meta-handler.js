// src/meta-handler.js

async function metaHandler({ type, id, config, cacheManager, epgManager, pythonResolver, pythonRunner }) {
    return {
        meta: {} // vazio por agora
    };
}

module.exports = metaHandler;
